name: PR Quality Check

on:
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  validate:
    name: Validate PR Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@60edb5dd545a775178f52524783378180af0d1f8 # v4.0.2
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      # Cek tipe data TypeScript tanpa build
      - name: TypeScript Check
        run: npx tsc --noEmit

      - name: Run Linting
        run: npm run lint

      - name: Security Audit
        run: npm audit --audit-level=high --omit=dev
        continue-on-error: true #sementara untuk melakukan testing

      - name: Dry Run Build
        run: npm run build
        env:
          NODE_ENV: production
